// lib/core/constants/app_constants.dart
import 'package:flutter/material.dart';

class AppColors {
  static const main = Color(0xFF222526);     // Onyx
  static const bg = Color(0xFF353A3E);       // Graphite
  static const light = Color(0xFFE0E0E0);    // Platinum
  static const dark = Color(0xFF1A1A1A);     // Jet Black
  static const hint = Color(0xFFBFBFBF);     // Ash
}

class AppFonts {
  static const String primaryFont = 'Poppins';
}
